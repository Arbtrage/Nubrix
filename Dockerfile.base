FROM python:3.11-slim

WORKDIR /app

# Install dependencies
RUN pip install fastapi uvicorn

# Copy the base API server
COPY base_server.py .

# Expose port 5000
EXPOSE 5000

# Run the server
CMD ["python", "-m", "uvicorn", "base_server:app", "--host", "0.0.0.0", "--port", "5000"] 